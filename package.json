{
  "name": "claude-discord-relay",
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "bin": "./src/bridge-server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun src/bridge-server.ts",
    "relay": "bun install --no-summary && bun src/relay-daemon.ts",
    "ctl": "bun src/relay-ctl.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "discord.js": "^14.14.0"
  }
}
